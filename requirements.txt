# Cross-platform dependency input. macOS release installs use the generated,
# hash-locked requirements-macos-arm64.txt file; do not install this input
# directly from setup-macos.sh.
pyaudiowpatch; sys_platform == "win32"
sounddevice; sys_platform == "darwin"
soxr
numpy
silero-vad
onnxruntime
kokoro-onnx; sys_platform != "win32"
faster-whisper
textual
tomli
nvidia-cublas-cu12; sys_platform != "darwin"
nvidia-cudnn-cu12; sys_platform != "darwin"
pytest
